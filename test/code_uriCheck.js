/*jshint node:true */
/*global describe:false, it:false */

'use strict';

var should = require('chai').should();
var rewire = require('rewire');
var manager = require('../code/uriCheck');

describe('code/uriCheck/', function() {
	describe('()', function () {
		it('', function (done) {
			done();
		});
	});
});
